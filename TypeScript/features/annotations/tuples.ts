const drink = {
    color: 'brown',
    carbonated: true,
    sugar: 40,
}

// tuple
const cola: [string, boolean, number] = ['brow', true, 40];

// type alias
type Drink = [string, boolean, number];
const sprite: Drink = ['black', false, 0];